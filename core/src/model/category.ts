/**
 * Categories used to group graphics settings in the menu.
 *
 * Categories are a presentation grouping only; they carry no behaviour. en-GB
 * spelling is used (e.g. "synchronisation").
 */
export type SettingCategory =
  | 'display'
  | 'upscaling'
  | 'textures'
  | 'lighting'
  | 'atmosphere'
  | 'post-processing'
  | 'synchronisation';
